#Q.Replace the double space from problem 3 with single spaces.

name="Jeevan is very very bad  boy and  "
print(name.replace("  "," "))

print(name)#Strings are immutable which means that you cannot change by running functions on them
